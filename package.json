{
  "name": "korb",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "reweCart": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run --root .",
    "lint": "tsc --noEmit",
    "ui:dev": "vite",
    "ui:build": "vite build",
    "ui:preview": "vite preview"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "@types/node": "^24.0.0",
    "preact": "^10.29.1",
    "typescript": "^5.8.0",
    "vitest": "^3.2.4"
  }
}
